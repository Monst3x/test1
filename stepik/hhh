#include <stdio.h>
#include <math.h>
float dist(int x1, int y1, int x2, int y2) 
{
    float ares, bres, res;
    ares = (x1-x2); // 3
    bres = (y1-y2); // -4
    res = ares*ares + bres*bres;
    printf("%d\n", bres);
   return res;
}
    